import { GameActionsPipe } from './game-actions.pipe';

describe('GameActionsPipe', () => {
  it('create an instance', () => {
    const pipe = new GameActionsPipe();
    expect(pipe).toBeTruthy();
  });
});
