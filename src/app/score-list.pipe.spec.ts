import { ScoreListPipe } from './score-list.pipe';

describe('ScoreListPipe', () => {
  it('create an instance', () => {
    const pipe = new ScoreListPipe();
    expect(pipe).toBeTruthy();
  });
});
